// src/app/(admin)/admin/break-screen/new/page.jsx
// Create a new Break Screen profile (generator form, create mode).
"use client";

import BreakScreenFormClient from "../_components/BreakScreenFormClient";

export default function NewBreakScreenProfilePage() {
  return <BreakScreenFormClient initial={null} />;
}
